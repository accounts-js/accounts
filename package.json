{
	"private": true,
	"name": "@accounts/workbench",
	"scripts": {
		"initial": "yarn install && lerna bootstrap",
		"start": "lerna exec --parallel -- yarn start",
		"clean": "lerna exec -- yarn clean",
		"clean_": "lerna exec -- yarn clean_",
		"link_global": "lerna exec -- yarn link",
		"unlink_global": "lerna exec -- yarn unlink",
		"compile": "lerna exec -- yarn run compile",
		"lint": "tslint packages/*/{src,__tests__}/**/*.ts"
	},
	"license": "MIT",
	"devDependencies": {
		"@types/node": "^8.0.54",
		"lerna": "^2.5.1",
		"tslint": "^5.8.0",
		"tslint-config-prettier": "^1.6.0",
		"tslint-eslint-rules": "^4.1.1",
		"typescript": "^2.6.2",
		"yarn": "^1.3.2"
	},
	"dependencies": {
		"rimraf": "^2.6.2"
	}
}
