[build]
  base    = "."
  publish = "website/build/accounts-js"
  command = "yarn build && yarn ts-node scripts/generate-api-docs.ts && cd website && yarn && yarn build"

[build.environment]
  NODE_VERSION = "9"
  YARN_VERSION = "1.10.1"
  YARN_FLAGS = "--pure-lockfile"
