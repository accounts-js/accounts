[build]
  base    = "."
  publish = "website/build"
  command = "yarn setup && cd website && yarn && yarn generate-api-docs && yarn build"

[build.environment]
  NODE_VERSION = "11"
  YARN_VERSION = "1.17.3"
  YARN_FLAGS = "--pure-lockfile"
