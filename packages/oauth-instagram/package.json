{
  "name": "@accounts/oauth-instagram",
  "version": "0.26.0-alpha.1",
  "license": "MIT",
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "scripts": {
    "clean": "rimraf lib",
    "start": "tsc --watch",
    "precompile": "yarn clean",
    "compile": "tsc",
    "prepublishOnly": "yarn compile",
    "testonly": "jest",
    "coverage": "jest --coverage"
  },
  "jest": {
    "testEnvironment": "node",
    "preset": "ts-jest"
  },
  "dependencies": {
    "@accounts/oauth": "^0.26.0-alpha.1",
    "request": "^2.88.0",
    "request-promise": "^4.2.5",
    "tslib": "1.10.0"
  },
  "devDependencies": {
    "@types/jest": "25.1.4",
    "@types/node": "13.9.8",
    "@types/request-promise": "4.1.44",
    "jest": "25.1.0"
  }
}
