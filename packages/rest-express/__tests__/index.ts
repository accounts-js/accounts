import TransportExpress from '../src';

describe('TransportExpress', () => {
  it('should have default export TransportExpress', () => {
    expect(typeof TransportExpress).toBe('function');
  });
});
