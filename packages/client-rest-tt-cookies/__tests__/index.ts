import ClientRestTTCookies from '../src';

describe('ClientRestTTCookies entry', () => {
  it('should have default export ClientRestTTCookies', () => {
    expect(typeof ClientRestTTCookies).toBe('function');
  });
});
