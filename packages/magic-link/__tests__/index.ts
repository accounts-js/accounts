import 'reflect-metadata';
import AccountsMagicLink from '../src';

describe('AccountsMagicLink', () => {
  it('should have default export AccountsMagicLink', () => {
    expect(typeof AccountsMagicLink).toBe('function');
  });
});
