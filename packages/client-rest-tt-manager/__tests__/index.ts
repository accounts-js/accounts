import ClientRestTTManager from '../src';

describe('ClientRestTTManager entry', () => {
  it('should have default export ClientRestTTManager', () => {
    expect(typeof ClientRestTTManager).toBe('function');
  });
});
