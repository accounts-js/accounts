import EmailPluginPassword from '../src';

describe('EmailPluginPassword entry', () => {
  it('should have default export EmailPluginPassword', () => {
    expect(typeof EmailPluginPassword).toBe('function');
  });
});
