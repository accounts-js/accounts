import ClientRestTTHeaders from '../src';

describe('ClientRestTTHeaders entry', () => {
  it('should have default export ClientRestTTHeaders', () => {
    expect(typeof ClientRestTTHeaders).toBe('function');
  });
});
