import CTSLocalStorage from '../src';

describe('CTSLocalStorage entry', () => {
  it('should have default export CTSLocalStorage', () => {
    expect(typeof CTSLocalStorage).toBe('function');
  });
});
